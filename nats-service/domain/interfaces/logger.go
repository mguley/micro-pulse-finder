package interfaces

const (
	// LogDir specifies the directory where log files are stored.
	LogDir = "logs"
	// LogFilePath specifies the file path for the NATS service log file.
	LogFilePath = LogDir + "/nats-service.log"
)
